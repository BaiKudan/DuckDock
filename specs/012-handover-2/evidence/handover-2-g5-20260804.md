# E06 Handover 2.0 / G5 技术验收证据

> 日期：2026-08-04
> 范围：HO-01～HO-08
> 环境：本机 Docker Compose、MySQL 8.4、MinIO、真实 Hermes Runtime #9、backend 8801、frontend 5174

## 结论

E06 已完成。既有可变交接流程之上新增了 append-only Evidence Snapshot、确定性依赖图、typed readiness、显式义务及回执、接收人接受和外部 Ed25519 签名包。签名私钥始终在外部，DuckDock 仅持有公钥、摘要、签名值和不可变 MinIO 证据回执。

E07 的真实目录 `DISABLE` 已完成 HO-08 集成缝：同一事务停用用户、撤销凭据、移除访问并自动创建/复用 HandoverCase。

## 可重复验证

```bash
docker compose exec -T -e PYTHONPATH=/app -e DEBUG=false \
  backend python scripts/verify_handover_2_dev.py
```

真实结果：

- HandoverCase `20`；后续目录 seam 自动生成 Case `21`；
- EvidenceSnapshot `hsnap_e83bfe06f764468d89a8c0ec9875f2c4`；
- SignedPackage `hpkg_170e380a80f34df187a05461230955c9`；
- package manifest、archive、signature、attestation 均为 SHA-256 摘要绑定；
- MinIO readback digest 与 manifest 一致；
- invalid/cross-Namespace/revoked key、未解决 blocking obligation 和冲突 replay 均 fail closed。

## 实现与门禁

- revision `20260804_0060`；空库升级、`alembic check`、空证据 downgrade/upgrade roundtrip 通过；已有不可变证据时 downgrade 按设计拒绝；
- `/api/v2/handover-*` 管理 API 和 `/handovers/:caseId` UI 完整展示 snapshot graph、readiness、obligation、acceptance 与 signed package；
- UI 使用真实记录，刷新后不丢失，浏览器 console error 为 `0`；
- metadata-only 边界禁止 prompt、输出正文、raw trace、token、私钥和 provider credential 进入 snapshot/package/Audit/Outbox；
- 最终全量回归：backend `1005 passed, 19 skipped, 3 warnings`；frontend `53 passed`，production build PASS。

## G5 判定

HO-01～HO-08、真实 MySQL/MinIO/Hermes、外部签名、目录停用联动和浏览器证据齐备，E06/G5 技术 Gate 通过。
